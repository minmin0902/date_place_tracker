# Agent instructions

Before making non-trivial code or UX changes in this repo, read `CLAUDE.md`
from the repo root. It is the shared project memory for Claude, Codex, and
future coding agents.

Especially read:
- `Pitfalls — patterns to avoid`
- `NotificationsPage architecture`
- `Performance budget`
- `When debugging a UI issue`

If a change contradicts, updates, or supersedes something documented there,
update `CLAUDE.md` in the same commit.

Current active local workspace:
`/Users/minjookim/Projects/date-place-tracker`

Local dev:
`npm run dev -- --host 127.0.0.1 --port 5173`
